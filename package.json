{
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "csso": "^4.2.0",
    "get-stdin": "^9.0.0"
  },
  "engines": {
    "node": ">=12.20",
    "npm": ">=7"
  }
}
