{
  "version": "3.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "csso": "^5.0.2",
    "get-stdin": "^9.0.0"
  },
  "engines": {
    "node": ">=16",
    "npm": ">=7"
  }
}
